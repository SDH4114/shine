# Shine Icons for Zed

Install this directory with Zed's `zed: install dev extension` command. Then
choose `Shine Icons` from `icon theme selector: toggle`. Files ending in
`.shn` use the repository logo from `shine.png`.
